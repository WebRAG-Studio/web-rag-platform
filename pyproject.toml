[project]
name = "sitemind"
version = "0.1.0"
description = "Turn websites into intelligent, source-grounded assistants."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 110
target-version = "py311"
